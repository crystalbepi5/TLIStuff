{
  "name": "torchlight-companion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.28.1",
  "scripts": {
    "dev": "pnpm --filter @torchlight-companion/local-agent dev",
    "build": "pnpm -r --sort build",
    "test": "pnpm -r --sort test",
    "typecheck": "pnpm build && pnpm -r --sort typecheck",
    "lint": "pnpm -r --sort lint"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "typescript": "^6.0.3"
  }
}
